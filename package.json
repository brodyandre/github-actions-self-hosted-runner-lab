{
  "name": "github-actions-self-hosted-runner-lab",
  "version": "0.1.0",
  "private": true,
  "description": "Laboratório simples para GitHub Actions com runners GitHub-hosted e self-hosted.",
  "main": "app/server.js",
  "scripts": {
    "start": "node app/server.js",
    "test": "node --test",
    "lint": "node --check app/config.js && node --check app/server.js && node --check test/server.test.js",
    "diagnostics": "./scripts/safe-diagnostics.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
